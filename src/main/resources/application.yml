spring:
  application:
    name: syscatalog
  datasource:
    url: jdbc:postgresql://localhost:5431/scat
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect